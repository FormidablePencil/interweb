package integrationTests.space.tests

import shared.testUtils.BehaviorSpecIT

class CmsLibOfCompsIT : BehaviorSpecIT() {
    init {
        val f = listOf("")
        f.forEach {
            given("create a single component $it") {

            }
        }

        given("create multiple components") {
            then("success") {

            }
        }
    }
}